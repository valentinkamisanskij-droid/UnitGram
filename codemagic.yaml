# This file is automatically generated by Codemagic.
# https://docs.codemagic.io/getting-started/yaml/

workflows:
  android-build:
    name: Android Build
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      # Add your Android signing properties here
      # groups:
      #   - keystore_credentials
      # keystore_credentials:
      #     CM_KEYSTORE: Encrypted(...) # Keystore file
      #     CM_KEYSTORE_PASSWORD: Encrypted(...) # Keystore password
      #     CM_KEY_ALIAS: Encrypted(...) # Key alias
      #     CM_KEY_ALIAS_PASSWORD: Encrypted(...) # Key alias password

    scripts:
      - name: Install dependencies
        script: |
          flutter pub get

      - name: Run static analysis
        script: |
          flutter analyze

      - name: Run tests
        script: |
          flutter test

      - name: Build Android APK
        script: |
          flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk

    publishing:
      email:
        recipients:
          - your_email@example.com # Replace with your email
        notify:
          success: true
          failure: false
